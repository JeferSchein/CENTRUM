class Fluxo{

	public boolean logico;
	
	public boolean logic igual(variavel b,int a){
		if(b.valor == a){
		      return logico = TRUE;
		}
		else{
		      return logico = FALSE;		
		}
	}		
	public boolean logic maior (variavel b, int a){
		if(b.valor > a){
			return logico = TRUE;	
		}	
		else{
			return logico = FALSE;		
		}
	}
	public boolean logic menor(variavel b,int a){
		if(b.valor < a){
			return logico = TRUE;	
		}	
		else{
			return logico = FALSE;		
		}	
	}
	
	
}
